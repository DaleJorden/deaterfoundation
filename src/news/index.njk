---
layout: layouts/base.njk
title: Impact Stories & News
description: Updates from the Deater Foundation community, including family stories, partner spotlights, and funding announcements.
pagination:
  data: collections.news
  size: 6
  alias: posts
permalink: "/news/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}"
---
<section class="bg-white">
  <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:px-8">
    <header class="max-w-3xl">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary-600">Impact stories</p>
      <h1 class="section-heading mt-4">Celebrating resilience and community care</h1>
      <p class="mt-4 text-sm text-slate-600">Follow the journeys of families, partners, and volunteers who transform generosity into life-changing support.</p>
    </header>
    <div class="mt-12 grid gap-8 md:grid-cols-2">
      {% for post in posts %}
        <article class="flex h-full flex-col rounded-3xl border border-slate-200 bg-white/70 p-8 shadow-card">
          <p class="text-xs font-semibold uppercase tracking-[0.2em] text-primary-500">{{ post.date | readableDate }}</p>
          <h2 class="mt-3 text-2xl font-semibold text-slate-900"><a class="hover:text-primary-600" href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <p class="mt-3 text-sm text-slate-600">{{ post.data.summary }}</p>
          <a class="btn-secondary mt-8 w-fit" href="{{ post.url }}">Continue reading</a>
        </article>
      {% endfor %}
    </div>
    {% if pagination.pages | length > 1 %}
      <nav class="mt-12 flex items-center justify-between" aria-label="Pagination">
        {% if pagination.href.previous %}
          <a class="btn-secondary" href="{{ pagination.href.previous }}">← Newer</a>
        {% else %}
          <span class="btn-secondary pointer-events-none opacity-50">← Newer</span>
        {% endif %}
        <span class="text-sm text-slate-500">Page {{ pagination.pageNumber + 1 }} of {{ pagination.pages | length }}</span>
        {% if pagination.href.next %}
          <a class="btn-secondary" href="{{ pagination.href.next }}">Older →</a>
        {% else %}
          <span class="btn-secondary pointer-events-none opacity-50">Older →</span>
        {% endif %}
      </nav>
    {% endif %}
  </div>
</section>
